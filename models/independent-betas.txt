model{
  for(i in 1:I){                       
    for(j in 1:J) {                    
      theta[i,j] <- (psi[j] + tau[i])/2 #or psi[j]*tau[i] 
      X[i,j] ~ dbern(theta[i,j])      
    }
  }
  
  for (i in 1:I) {
    tau[i] ~ dbeta(3,3) 
  }
    
  for (j in 1:J) {
    psi[j] ~ dbeta(3,3) 
  }
}